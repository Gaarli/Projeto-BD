# Configurações do Banco de Dados (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=eletroreverso
DB_USER=postgres
# Digite abaixo a senha do seu banco de dados local para testar a aplicação
DB_PASSWORD=insira_sua_senha_local_aqui

# Configurações do Servidor (Flask)
PORT=5000
FLASK_DEBUG=true